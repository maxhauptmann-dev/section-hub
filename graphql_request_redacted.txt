# Redacted GraphQL request payload — themeFilesUpsert
# All tokens, shop names, theme IDs and long identifiers are replaced with [REDACTED].

POST https://[REDACTED].myshopify.com/admin/api/2025-10/graphql.json
Headers:
  Authorization: Bearer [REDACTED]
  Content-Type: application/json

Payload (GraphQL):

mutation ThemeFilesUpsert($input: ThemeFilesUpsertInput!) {
  themeFilesUpsert(input: $input) {
    userErrors { field message }
    upsertedThemeFiles {
      id
      key
      # 'status' was removed from our query to match the API
    }
  }
}

Variables (redacted example):
{
  "input": {
    "themeId": "[REDACTED]",
    "files": [
      {
        "key": "sections/hero-minimal.liquid",
        "content": "<!-- section liquid content redacted -->"
      },
      {
        "key": "sections/cta-banner.liquid",
        "content": "<!-- section liquid content redacted -->"
      }
    ]
  }
}

Notes:
- The actual request body sent by the app will mirror the mutation above. Replace the example file contents with the real payload if you have it.
- If you want, I can attempt to extract a more precise payload from the full logs or from a copied DevTools Network request you paste here; I won't include any secrets when redacting.
